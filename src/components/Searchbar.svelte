<input type="text" placeholder="Search.." />

<style>
	input {
		height: 26px;
		width: 45%;
		background: var(--primary-light);
		color: var(--text-primary);
		border: none;
		border-radius: 4px;
		padding: 0 15px;
	}

	input::placeholder {
		color: #dfdfdf;
	}

	input:focus {
		outline: none;
	}
</style>
